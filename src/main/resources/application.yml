spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/estoque_db
        username: joaogstex
        password: euAmoAndressaEStarWars
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect